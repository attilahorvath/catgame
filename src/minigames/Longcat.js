export default class {
  // TODO: Background grid, not interactive, place target in random cell (avoid snake), snake sprite made up of segments, store pos/angle, circular buffer, add segment when target got
}
